"""
content resource operations.

Generated by Forge SDK Generator.
"""

from typing import Optional, List, Dict, Any, Union, Iterator, AsyncIterator
from .._http import HttpClient, AsyncHttpClient
from ..models import *
from ..exceptions import raise_for_status


class ContentResource:
    """content resource operations."""

    def __init__(self, http: HttpClient):
        self._http = http

    def list_datasource_content(self, datasource_id: str, prefix: Optional[Union[str, None]] = None, continuation_token: Optional[Union[str, None]] = None, limit: Optional[int] = None) -> "ListContentResponse":
        """
        List datasource content
        
        List files and directories in a datasource with optional filtering and pagination
        
        Args:
            datasource_id: The datasource_id parameter
            prefix: Filter content by path prefix
            continuation_token: Token for pagination to get next page of results
            limit: Maximum number of items to return (1-10000)
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content"
        path = path.replace("{datasource_id}", str(datasource_id))
        params = {}
        if prefix is not None:
            params["prefix"] = prefix
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if limit is not None:
            params["limit"] = limit
        response = self._http.request("GET", path, params=params)
        return ListContentResponse.model_validate(response)

    def upload_datasource_content(self, datasource_id: str) -> "UploadContentResponse":
        """
        Upload Content
        
        Proxy upload with zero-copy streaming.
        
        This endpoint maintains the multipart form data structure and streams
        it directly to the backend service without buffering files in memory.
        The multipart parsing happens on the backend service side.
        
        Args:
            datasource_id: The datasource_id parameter
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = self._http.request("POST", path)
        return UploadContentResponse.model_validate(response)

    def stream_upload_progress(self, upload_id: str) -> None:
        """
        Stream upload progress events
        
        Subscribe to real-time upload progress updates via Server-Sent Events
        
        Args:
            upload_id: The upload_id parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/uploads/{upload_id}/progress"
        path = path.replace("{upload_id}", str(upload_id))
        response = self._http.request("GET", path)
        return response

    def get_datasource_upload_status(self, datasource_id: str, upload_id: str) -> str:
        """
        Get upload status
        
        Get the current status of a content upload operation
        
        Args:
            datasource_id: The datasource_id parameter
            upload_id: The upload_id parameter
        
        Returns:
            Upload status information
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/upload-status/{upload_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{upload_id}", str(upload_id))
        response = self._http.request("GET", path)
        return response

    def stream_datasource_upload_progress(self, datasource_id: str, upload_id: str) -> None:
        """
        Stream upload progress events (legacy)
        
        Subscribe to real-time upload progress updates via Server-Sent Events. Consider using /uploads/{upload_id}/progress instead.
        
        Args:
            datasource_id: The datasource_id parameter
            upload_id: The upload_id parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/upload-progress/{upload_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{upload_id}", str(upload_id))
        response = self._http.request("GET", path)
        return response

    def get_datasource_content_metadata(self, datasource_id: str, path: str) -> "GetContentResponse":
        """
        Get content metadata
        
        Get metadata information for a file or directory in the datasource
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            Content metadata information
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = self._http.request("GET", path)
        return GetContentResponse.model_validate(response)

    def delete_datasource_content(self, datasource_id: str, path: str) -> "DeleteContentResponse":
        """
        Delete content
        
        Delete a file or directory from the datasource
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            Content successfully deleted
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = self._http.request("DELETE", path)
        return DeleteContentResponse.model_validate(response)

    def download_datasource_content(self, datasource_id: str, path: str) -> None:
        """
        Download content file
        
        Download a file from the datasource with streaming support for large files
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}/download"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = self._http.request("GET", path)
        return response


class AsyncContentResource:
    """content resource operations (async)."""

    def __init__(self, http: AsyncHttpClient):
        self._http = http

    async def list_datasource_content(self, datasource_id: str, prefix: Optional[Union[str, None]] = None, continuation_token: Optional[Union[str, None]] = None, limit: Optional[int] = None) -> "ListContentResponse":
        """
        List datasource content
        
        List files and directories in a datasource with optional filtering and pagination
        
        Args:
            datasource_id: The datasource_id parameter
            prefix: Filter content by path prefix
            continuation_token: Token for pagination to get next page of results
            limit: Maximum number of items to return (1-10000)
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content"
        path = path.replace("{datasource_id}", str(datasource_id))
        params = {}
        if prefix is not None:
            params["prefix"] = prefix
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if limit is not None:
            params["limit"] = limit
        response = await self._http.request("GET", path, params=params)
        return ListContentResponse.model_validate(response)

    async def upload_datasource_content(self, datasource_id: str) -> "UploadContentResponse":
        """
        Upload Content
        
        Proxy upload with zero-copy streaming.
        
        This endpoint maintains the multipart form data structure and streams
        it directly to the backend service without buffering files in memory.
        The multipart parsing happens on the backend service side.
        
        Args:
            datasource_id: The datasource_id parameter
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = await self._http.request("POST", path)
        return UploadContentResponse.model_validate(response)

    async def stream_upload_progress(self, upload_id: str) -> None:
        """
        Stream upload progress events
        
        Subscribe to real-time upload progress updates via Server-Sent Events
        
        Args:
            upload_id: The upload_id parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/uploads/{upload_id}/progress"
        path = path.replace("{upload_id}", str(upload_id))
        response = await self._http.request("GET", path)
        return response

    async def get_datasource_upload_status(self, datasource_id: str, upload_id: str) -> str:
        """
        Get upload status
        
        Get the current status of a content upload operation
        
        Args:
            datasource_id: The datasource_id parameter
            upload_id: The upload_id parameter
        
        Returns:
            Upload status information
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/upload-status/{upload_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{upload_id}", str(upload_id))
        response = await self._http.request("GET", path)
        return response

    async def stream_datasource_upload_progress(self, datasource_id: str, upload_id: str) -> None:
        """
        Stream upload progress events (legacy)
        
        Subscribe to real-time upload progress updates via Server-Sent Events. Consider using /uploads/{upload_id}/progress instead.
        
        Args:
            datasource_id: The datasource_id parameter
            upload_id: The upload_id parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/upload-progress/{upload_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{upload_id}", str(upload_id))
        response = await self._http.request("GET", path)
        return response

    async def get_datasource_content_metadata(self, datasource_id: str, path: str) -> "GetContentResponse":
        """
        Get content metadata
        
        Get metadata information for a file or directory in the datasource
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            Content metadata information
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = await self._http.request("GET", path)
        return GetContentResponse.model_validate(response)

    async def delete_datasource_content(self, datasource_id: str, path: str) -> "DeleteContentResponse":
        """
        Delete content
        
        Delete a file or directory from the datasource
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            Content successfully deleted
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = await self._http.request("DELETE", path)
        return DeleteContentResponse.model_validate(response)

    async def download_datasource_content(self, datasource_id: str, path: str) -> None:
        """
        Download content file
        
        Download a file from the datasource with streaming support for large files
        
        Args:
            datasource_id: The datasource_id parameter
            path: The path parameter
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasource/{datasource_id}/content/{path}/download"
        path = path.replace("{datasource_id}", str(datasource_id))
        path = path.replace("{path}", str(path))
        response = await self._http.request("GET", path)
        return response
