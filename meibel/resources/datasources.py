"""
Datasources resource operations.

Generated by Forge SDK Generator.
"""

from typing import Optional, List, Dict, Any, Union, Iterator, AsyncIterator
from .._http import HttpClient, AsyncHttpClient
from ..models import *
from ..exceptions import raise_for_status
from .._metadata import metadata_schema_from_model
from .data_elements import DataElementsResource, AsyncDataElementsResource
from .downloads import DownloadsResource, AsyncDownloadsResource
from .file_uploads import FileUploadsResource, AsyncFileUploadsResource
from .ingest import IngestResource, AsyncIngestResource
from .tables import TablesResource, AsyncTablesResource


class DatasourcesResource:
    """Manage data source connections"""

    def __init__(self, http: HttpClient):
        self._http = http
        self.data_elements = DataElementsResource(http)
        self.downloads = DownloadsResource(http)
        self.file_uploads = FileUploadsResource(http)
        self.ingest = IngestResource(http)
        self.tables = TablesResource(http)

    def list(self) -> "DatasourceListResponse":
        """
        List Datasources
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources"
        response = self._http.request("GET", path)
        return DatasourceListResponse.model_validate(response)

    def create(self, name: str, connector: "ConnectorConfig", description: str = "", metadata_config: Optional[Union[Dict[str, Any], Type[BaseModel]]] = None) -> "DatasourceResponse":
        """
        Create Datasource
        
        Args:
            body: Request body
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources"
        if isinstance(metadata_config, type) and issubclass(metadata_config, BaseModel):
            _metadata_config = metadata_schema_from_model(metadata_config)
        else:
            _metadata_config = metadata_config
        body = CreateDatasourceRequest(
            name=name,
            description=description,
            connector=connector,
            metadata_config=_metadata_config,
        )
        response = self._http.request("POST", path, json=body.model_dump(by_alias=True, exclude_unset=True))
        return DatasourceResponse.model_validate(response)

    def get(self, datasource_id: str, include_tables: Optional[bool] = None) -> "DatasourceResponse":
        """
        Get Datasource
        
        Args:
            datasource_id: The datasource_id parameter
            include_tables: Include table and column details (structured datasources only)
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        params = {}
        if include_tables is not None:
            params["include_tables"] = include_tables
        response = self._http.request("GET", path, params=params)
        return DatasourceResponse.model_validate(response)

    def update(self, datasource_id: str, body: "UpdateDatasourceRequest") -> "DatasourceResponse":
        """
        Update Datasource
        
        Args:
            datasource_id: The datasource_id parameter
            body: Request body
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = self._http.request("PUT", path, json=body.model_dump(by_alias=True, exclude_unset=True) if body else None)
        return DatasourceResponse.model_validate(response)

    def delete(self, datasource_id: str) -> "DeleteDatasourceResponse":
        """
        Delete Datasource
        
        Args:
            datasource_id: The datasource_id parameter
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = self._http.request("DELETE", path)
        return DeleteDatasourceResponse.model_validate(response)

    def chat_with(self, body: "ChatWithDatasourceRequest") -> None:
        """
        Chat with datasources via AI (streaming)
        
        Ask a question against one or more datasources. Returns a streaming SSE response with the AI-generated answer.
        
        Args:
            body: Request body
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/chat"
        response = self._http.request("POST", path, json=body.model_dump(by_alias=True, exclude_unset=True) if body else None)
        return response


class AsyncDatasourcesResource:
    """Manage data source connections (async)"""

    def __init__(self, http: AsyncHttpClient):
        self._http = http
        self.data_elements = AsyncDataElementsResource(http)
        self.downloads = AsyncDownloadsResource(http)
        self.file_uploads = AsyncFileUploadsResource(http)
        self.ingest = AsyncIngestResource(http)
        self.tables = AsyncTablesResource(http)

    async def list(self) -> "DatasourceListResponse":
        """
        List Datasources
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources"
        response = await self._http.request("GET", path)
        return DatasourceListResponse.model_validate(response)

    async def create(self, name: str, connector: "ConnectorConfig", description: str = "", metadata_config: Optional[Union[Dict[str, Any], Type[BaseModel]]] = None) -> "DatasourceResponse":
        """
        Create Datasource
        
        Args:
            body: Request body
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources"
        if isinstance(metadata_config, type) and issubclass(metadata_config, BaseModel):
            _metadata_config = metadata_schema_from_model(metadata_config)
        else:
            _metadata_config = metadata_config
        body = CreateDatasourceRequest(
            name=name,
            description=description,
            connector=connector,
            metadata_config=_metadata_config,
        )
        response = await self._http.request("POST", path, json=body.model_dump(by_alias=True, exclude_unset=True))
        return DatasourceResponse.model_validate(response)

    async def get(self, datasource_id: str, include_tables: Optional[bool] = None) -> "DatasourceResponse":
        """
        Get Datasource
        
        Args:
            datasource_id: The datasource_id parameter
            include_tables: Include table and column details (structured datasources only)
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        params = {}
        if include_tables is not None:
            params["include_tables"] = include_tables
        response = await self._http.request("GET", path, params=params)
        return DatasourceResponse.model_validate(response)

    async def update(self, datasource_id: str, body: "UpdateDatasourceRequest") -> "DatasourceResponse":
        """
        Update Datasource
        
        Args:
            datasource_id: The datasource_id parameter
            body: Request body
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = await self._http.request("PUT", path, json=body.model_dump(by_alias=True, exclude_unset=True) if body else None)
        return DatasourceResponse.model_validate(response)

    async def delete(self, datasource_id: str) -> "DeleteDatasourceResponse":
        """
        Delete Datasource
        
        Args:
            datasource_id: The datasource_id parameter
        
        Returns:
            Successful Response
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/{datasource_id}"
        path = path.replace("{datasource_id}", str(datasource_id))
        response = await self._http.request("DELETE", path)
        return DeleteDatasourceResponse.model_validate(response)

    async def chat_with(self, body: "ChatWithDatasourceRequest") -> None:
        """
        Chat with datasources via AI (streaming)
        
        Ask a question against one or more datasources. Returns a streaming SSE response with the AI-generated answer.
        
        Args:
            body: Request body
        
        Returns:
            None
        
        Raises:
            ApiError: If the request fails
        """
        path = "/datasources/chat"
        response = await self._http.request("POST", path, json=body.model_dump(by_alias=True, exclude_unset=True) if body else None)
        return response
